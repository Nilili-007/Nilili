import { RegisterForm } from "../components/auth";

const Register = () => {
  return (
    <div className="flex flex-wrap flex-col items-center h-[1100px]">
      <RegisterForm />
    </div>
  );
};

export default Register;
